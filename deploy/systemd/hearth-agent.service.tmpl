[Unit]
Description=Hearth agent (container lifecycle)
After=podman.socket
Wants=podman.socket

[Service]
Type=simple
WorkingDirectory=@REPO@
EnvironmentFile=@REPO@/.env
ExecStart=@REPO@/target/release/hearth-agent
Restart=on-failure
RestartSec=2

[Install]
WantedBy=default.target
